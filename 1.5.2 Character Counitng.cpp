#include<stdio.h>
//Count characters in input; 2st version

int main(){
	
	double nc;
	
	for (nc = 0; getchar() != EOF; ++nc); // ++ ,means incremented by one
	
	printf("%.Of\n", nc);
	
}
